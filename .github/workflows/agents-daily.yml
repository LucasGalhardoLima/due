name: "Agent: Tech Debt (Weekly)"

on:
  schedule:
    # Runs Wednesday 8am BRT (11:00 UTC) — same day as Security agent
    - cron: "0 11 * * 3"
  workflow_dispatch:

jobs:
  tech-debt:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 10

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Run Tech Debt agent
        run: npx tsx scripts/agents/tech-debt.ts
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          LINEAR_API_KEY: ${{ secrets.LINEAR_API_KEY }}
